<section id="demo" class="py-20 border-t border-brand-hairline dark:border-brand-border/80 transition-colors">
  <div class="max-w-6xl mx-auto px-6">
    <div class="reveal text-center max-w-3xl mx-auto mb-12">
      <div class="text-xs font-mono text-brand-accent tracking-[0.2em] uppercase mb-4">Demo</div>
      <h2 class="text-3xl font-semibold tracking-tight text-ink dark:text-ivory" data-i18n="demo_title">ReAct 循环演示</h2>
      <p class="text-brand-muted mt-3" data-i18n="demo_subtitle">一段真实录屏，展示从用户输入到 &lt;FINAL_ANSWER&gt; 的 ReAct 循环：统一引擎驱动，权限看门狗把关，Code Mode 编排工具。</p>
    </div>

    <div class="max-w-4xl mx-auto space-y-10">
      <!-- 真实录屏 -->
      <div class="reveal terminal-dark-box border border-brand-border rounded-2xl overflow-hidden">
        <div class="bg-brand-black px-4 py-2.5 border-b border-brand-border flex items-center gap-3">
          <div class="flex items-center gap-1.5" aria-hidden="true">
            <span class="w-3 h-3 rounded-full bg-red-500/80"></span>
            <span class="w-3 h-3 rounded-full bg-yellow-500/80"></span>
            <span class="w-3 h-3 rounded-full bg-green-500/80"></span>
          </div>
          <span class="text-[10px] font-mono text-brand-muted" data-i18n="demo_video_caption">真实录屏演示</span>
        </div>
        <video
          controls
          loop
          playsinline
          preload="metadata"
          class="w-full h-auto block"
          data-i18n-aria="demo_video_aria"
          aria-label="Auraxis Agent ReAct 循环真实录屏"
        >
          <source src={`${import.meta.env.BASE_URL}videos/react-demo.mp4`} type="video/mp4" />
        </video>
      </div>

    </div>
  </div>
</section>
